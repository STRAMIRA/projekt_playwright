# Playwright – automatizované testy

Projekt obsahuje 3 automatizované testy webu https://engeto.cz
napsané pomocí Playwright a pytest-playwright.

## Spuštění testů
pip install pytest playwright pytest-playwright
playwright install
pytest -v